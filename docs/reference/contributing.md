# Contributing

The canonical contributor guide is in the repo root: `CONTRIBUTING.md`.

Key rules to remember:

- Update docs first; code follows.
- Any cross-boundary change (net/security/data/CI/dashboard) must update docs and may require an ADR.
- `main` is a protected branch — all changes must go through a pull request with the `build` CI check passing.
